Создаем задания и загружаем их в текстовый файл с заданиями.
Выгружаем их и закидываем  в спец арейлист под них.

Создаем работников в текстовом файле и выгружаем их также в арейлист созданный под них в мейне.

Задаем цену для еды в столовой.

Создаем потоки которые будем использовать от начала программы до конца.

Задаем переменную которая будет отнимать ее значение от параметра здоровья у работника в случае не попадания в столовую за одну иттерацию.

Задаем время, через сколько будем проверять список работников.

Выгрузим оставшихся работников обратно в текстовый файл .

Те работы которые были выполненны тоже должны быть удалены и потом загруженны в текстовой файл.

Примечание : Экспа работников растет в случае успешного выполнения задания и деньги тоже.
